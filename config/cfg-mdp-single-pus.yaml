agent:
    type: "MDP"

environment:
    episodes: 1
    job_horizon: 10
    generation_scheme: "single"
    num_machine: 1
    scheduling: "non-preemptive"
     
job_generator:
    name: "Uniform"
    parameters:
        low: 1
        high: 5
        func: "lambda x: x * x"
